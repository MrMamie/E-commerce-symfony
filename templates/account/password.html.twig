{% extends 'base.html.twig' %}

{% block title %}
Mon compte -  Art2pix{% endblock %}

{% block content %}
    <h1>Modifier votre mot de passe</h1>
    <a href="{{path('app_account')}}">Retourner à mon compte</a>
    <hr>
    {% if notification %}
        <div class="alert alert-info">{{notification}}</div>
    {% endif %}
    {{ form(form) }} 
{% endblock %}
